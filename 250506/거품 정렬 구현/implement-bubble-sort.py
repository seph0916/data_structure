n = int(input())
arr = list(map(int, input().split()))

# Please write your code here.


def bubble_sort(num, ls)i:
    for i in range(n):
        for j in range(n - 1 - i):
            if arr[j] > arr[j+1]
                data = arr[j]
                arr[j] = arr[j+1]
                arr[j+1] = data
    return arr

for d in arr:
    print(d, end=" ")
